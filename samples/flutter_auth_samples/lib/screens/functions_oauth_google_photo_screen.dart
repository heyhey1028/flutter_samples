import 'package:flutter/material.dart';

class FunctionsOauthGooglePhotoScreen extends StatelessWidget {
  const FunctionsOauthGooglePhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Functions Oauth Google Photo Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
