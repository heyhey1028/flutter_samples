import 'package:flutter/material.dart';

class HttpGooglePhotoScreen extends StatelessWidget {
  const HttpGooglePhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Http Google Photo Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
