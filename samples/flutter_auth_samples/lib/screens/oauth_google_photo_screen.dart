import 'package:flutter/material.dart';

class OauthGooglePhotoScreen extends StatelessWidget {
  const OauthGooglePhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Oauth Google Photo Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
