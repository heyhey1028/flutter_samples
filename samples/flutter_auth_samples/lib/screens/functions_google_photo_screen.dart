import 'package:flutter/material.dart';

class FunctionsGooglePhotoScreen extends StatelessWidget {
  const FunctionsGooglePhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Functions Google Photo Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
